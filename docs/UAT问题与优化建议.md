# UAT 问题与优化建议

> 来源：`docs/JL系统前端走查报告/`。本表基于当前代码、现有自动化测试和走查证据复核。

| 编号 | 类型 | 模块 | 当前行为 | 期望行为 | 严重程度 | 使用频率 | 是否建议在 V1.0.0 前处理 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| UAT-001 | Bug | 响应式导航 | 走查截图显示窄屏抽屉空白；当前代码已经使用独立移动抽屉，具备遮罩、Esc 关闭和移动端回归测试。 | 窄屏打开导航后应能看到全部可达入口。 | P0 | 高 | 否（已修复并回归） |
| UAT-002 | Bug | 构建与部署 | 已增加 `make release VERSION=...` 正式构建入口，并阻止 `APP_ENV=production` 使用 `make dev`；生产服务器尚未在本次 UAT 中部署。 | 生产环境应只发布 `vite build` 产物，由 Nginx 或后端静态托管，开发服务器不得作为生产入口。 | P0 | 高 | 是（正式发布验证仍需执行） |
| UAT-003 | Bug | 全站资源 | 原报告发现 favicon 缺失；当前已增加 `frontend/public/favicon.svg` 并在 HTML 中声明。未认证时 `/api/auth/me` 返回 401 是预期的登录状态探针，不是业务请求失败。 | 浏览器不应因 favicon 产生 404；认证探针应被视为未登录状态处理。 | P1 | 高 | 否（已修复） |
| UAT-004 | Bug | 移动端布局 | 当前导航主内容在移动端已有底部安全留白，移动壳回归通过；原截图中的底部遮挡未在当前实现复现。 | 固定底部导航不应遮挡最后一行内容、按钮或空状态。 | P1 | 高 | 否（已修复并回归） |
| UAT-005 | Bug | 日历与学习中心本地化 | 当前日历使用中文 locale、一周从周一开始；学习类型、管理小标题和统计粒度已使用中文映射。 | 面向中文用户的可见枚举、月份、星期和页面小标题应统一中文，后端枚举只用于数据传输。 | P1 | 高 | 否（已修复） |
| UAT-006 | 优化 | 团队关系图 | 团队关系图已改为应用内轻量关系画布，移除 React Flow attribution，不再遮挡业务内容；目标关系图仍继续使用既有 React Flow。 | 在授权允许时隐藏；否则保留且不遮挡业务内容。 | P1 | 中 | 否（已处理） |
| UAT-007 | Bug | 团队关系图 | 当前节点有明确的父子 Handle、可读的级别和城市字段，边使用成员 ID 连接；原截图中的悬空线和“1·1”短标签已被实现替换。 | 父子节点应真实连接，节点副标题应能解释业务字段。 | P1 | 中 | 否（已修复，需在真实数据上继续观察） |
| UAT-008 | Bug | 营业额与今日工作表单 | 当前营业额和工作量表单都有非负数约束、PV 与金额的舍入校验、数字输入模式和即时重新校验，并在字段旁显示错误；业务规则仍由后端兜底。 | 负数和 PV/金额不一致时应及时显示可读错误，不应提交。 | P1 | 高 | 否（已修复） |
| UAT-009 | Bug | 数据统计 | 当前所有聚合值为零时显示统一“当前区间暂无数据”空状态，不再渲染几像素的假图表。 | 无真实数据时应明确提示空状态；有真实零值时不能伪造非零图形。 | P2 | 中 | 否（已修复） |
| UAT-010 | 新需求 | 全局搜索 | 已增加目标、日历、团队、学习、标签五个搜索范围，按账号保存最近五条搜索，并按模块分组显示结果；后端同步按模块过滤。 | 用户可以控制搜索范围，查看最近搜索，结果按模块分组，并保持当前账号数据边界。 | P2 | 中 | 否（已实现） |
| UAT-011 | 优化 | 全站按钮 | 共享 Button 已统一尺寸、颜色、危险态、图标策略、禁用态、加载态和 `aria-busy`；主要操作按钮已补充语义图标。 | 同一层级的按钮应保持统一尺寸、图标策略、禁用态和操作语义。 | P2 | 高 | 否（已实现） |
| UAT-012 | 新需求 | 业务术语 | 收入模拟和今日工作量已增加 PV、BFI、BBI、Buffer、行动总数等术语帮助和计算口径面板，文案明确标注当前规则版本。 | 术语应提供可理解的定义、计算口径和展示文案。 | P2 | 中 | 否（已实现；规则变更时需同步文案） |
| UAT-013 | 优化 | 浏览器标题 | 当前标题按路由显示“模块名 · JL团队生意成长管理系统”，不再使用固定用户名标题。 | 多标签页应能通过标题区分模块。 | P2 | 中 | 否（已修复） |
| UAT-014 | 优化 | 顶栏健康状态 | API 健康检查成功时当前不显示技术徽标，仅在检测中或不可用时提示用户。 | 正常状态不打扰用户，异常时给出可理解的网络提示。 | P2 | 中 | 否（已修复） |
| UAT-015 | 优化 | 页面加载 | 当前统一 LoadingState 使用骨架结构和可访问状态，不再只显示旋转图标；懒加载页面仍会有短暂骨架。 | 加载态应稳定、可识别，并尽量贴近目标页面结构。 | P2 | 高 | 否（已修复） |
| UAT-016 | Bug | 可访问性 | 密码表单已补隐藏用户名和 autocomplete；导航、删除、文件等图标按钮已有 aria-label，团队关系图也补充了 aria-label。 | 密码管理器和读屏用户应能识别账号、密码和图标操作。 | P2 | 中 | 否（已修复；仍需持续做真实读屏验收） |
| UAT-017 | 优化 | 今日工作量 | 已按顾客行动、学习投入、营业额分组，并增加行动总数、学习总分钟和 PV/净额折合金额实时摘要；数据仍只提交到既有工作记录事实源。 | 用分组标题和摘要降低长表单录入负担，且不改变事实源和计算规则。 | P3 | 高 | 否（已实现） |
| UAT-018 | 优化 | 日历 | 当前已将“今天”改为主题色淡底，并弱化非本月日期颜色。 | 今天应在墨绿主题中清晰可见，非本月日期应降低视觉权重。 | P3 | 中 | 否（已修复） |
| UAT-019 | 优化 | 收入模拟 | 12 个市场使用响应式网格，已增加批量清零、复制上一方案、勾选多方案比较和差异表；未修改收入计算公式。 | 在不改变收入公式的前提下，进一步提高多市场录入和方案比较效率。 | P3 | 中 | 否（已实现） |
| UAT-020 | 优化 | 登录与注册 | 当前移动端优先显示登录或注册表单，产品介绍内容后置，保留现有可访问名称和切换入口。 | 窄屏首屏应优先完成登录或注册，说明内容不应把表单推得过低。 | P3 | 高 | 否（已修复） |
| UAT-021 | 新需求 | 团队成员数据质量 | 已采用保守规则：姓名去除首尾空白后必须为 2 到 200 个字符，且不能全部为 ASCII 数字；前后端均校验并显示可读错误，不限制正常中文、英文或混合姓名。 | 拦截明显无效姓名，同时保留正常中文、英文和混合姓名。 | P3 | 中 | 否（已实现） |

## 本次已实施的真实问题修复

- 增加 favicon，并修正成功健康检查和加载态的用户呈现。
- 修正中文化残留，包括日历 locale、学习类型、管理导航和统计粒度。
- 改进团队关系图的父子 Handle、节点信息、可访问名称和 attribution 位置。
- 为数字输入补充非负约束、数字输入模式和 PV/金额一致性校验。
- 空统计图显示统一空状态，页面标题按路由区分，移动端认证页优先展示表单。
- 补充密码表单账号关联信息和相关测试断言。

## 收口说明

- 本次已按授权执行全部 21 项 UAT Bug、优化和新需求，不再保留“暂不实施”的代码项。
- UAT-002 的代码防护和正式构建入口已完成；正式生产发布仍需在目标服务器执行发布演练，但本次任务不部署生产环境。
- UAT-012、UAT-021 已采用当前产品规则的保守文案和校验边界；后续若产品规则变化，需要同步修改前后端校验及帮助文案。
