# Deployment Templates

Production deployment uses Nginx and systemd, not Docker. Templates and release scripts belong in this directory tree and `scripts/`; they must never embed production credentials.

The Phase 0 baseline establishes the expected directory while application deployment behavior remains out of scope until the production release preparation work is implemented.
