import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Check, Sparkles, Trash2 } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useForm } from "react-hook-form";
import { z } from "zod";
import { zodResolver } from "@hookform/resolvers/zod";
import { Background, Controls, MiniMap, ReactFlow, type Edge, type Node } from "@xyflow/react";
import "@xyflow/react/dist/style.css";
import type { AuthResponse, Goal, GoalRequest } from "@/api/client";
import { deleteDream, deleteGoal, listDreams, listGoals, saveDream, saveGoal } from "@/api/client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Panel } from "@/components/ui/panel";
import { PageHeader } from "@/components/ui/page-header";
import { ConfirmDialog } from "@/components/ui/dialog";
import { StatusBadge } from "@/components/ui/badge";
import { EmptyState, ErrorState, LoadingState } from "@/components/ui/state-block";
import { errorMessage } from "@/lib/utils";

const goalTypes = ["LONG_TERM", "YEAR", "STAGE", "MONTH", "WEEK", "DAY"] as const;
const metricCodes = ["conversation_open_count", "deep_conversation_count", "buffer_count", "story_share_count", "screening_count", "opportunity_count", "meeting_count", "customer_followup_count", "reading_minutes", "audio_minutes", "turnover_pv", "turnover_net_amount"] as const;
const goalSchema = z.object({ title: z.string().trim().min(1, "请输入目标名称").max(200), type: z.enum(goalTypes), parent_id: z.string(), start_date: z.string(), due_date: z.string(), status: z.enum(["NOT_STARTED", "IN_PROGRESS", "COMPLETED", "PAUSED", "CANCELLED"]), metric_code: z.string(), target_value: z.number().finite().min(0).optional(), unit: z.string().max(32) });
const dreamSchema = z.object({ title: z.string().trim().min(1, "请输入梦想名称").max(200), description: z.string() });
type GoalForm = z.infer<typeof goalSchema>;
type DreamForm = z.infer<typeof dreamSchema>;

const typeLabels: Record<string, string> = { LONG_TERM: "长期", YEAR: "年度", STAGE: "阶段", MONTH: "月度", WEEK: "周", DAY: "日" };
const statusLabels: Record<string, string> = { NOT_STARTED: "未开始", IN_PROGRESS: "进行中", COMPLETED: "已完成", PAUSED: "已暂停", CANCELLED: "已取消" };
const statusTones: Record<string, "success" | "warning" | "danger" | "neutral" | "info"> = { NOT_STARTED: "neutral", IN_PROGRESS: "info", COMPLETED: "success", PAUSED: "warning", CANCELLED: "danger" };

function defaultGoal(): GoalForm { return { title: "", type: "YEAR", parent_id: "", start_date: "", due_date: "", status: "NOT_STARTED", metric_code: "", target_value: undefined, unit: "" }; }
function goalForm(value: Goal): GoalForm { const metric = value.metrics[0]; return { title: value.title, type: value.type, parent_id: value.parent_id ?? "", start_date: value.start_date ?? "", due_date: value.due_date ?? "", status: value.status, metric_code: metric?.metric_code ?? "", target_value: metric?.target_value, unit: metric?.unit ?? "" }; }
function defaultDream(): DreamForm { return { title: "", description: "" }; }

export function GoalsPage({ authResponse }: { authResponse: AuthResponse }) {
  const accountID = authResponse.data.account.id;
  const queryClient = useQueryClient();
  const goalsQuery = useQuery({ queryKey: ["user", accountID, "goals"], queryFn: listGoals });
  const dreamsQuery = useQuery({ queryKey: ["user", accountID, "dreams"], queryFn: listDreams });
  const [editing, setEditing] = useState<Goal | null>(null);
  const [selected, setSelected] = useState<Goal | null>(null);
  const [deleteTarget, setDeleteTarget] = useState<{ kind: "goal" | "dream"; id: string; title: string } | null>(null);
  const [notice, setNotice] = useState("");
  const [error, setError] = useState("");
  const goalFormState = useForm<GoalForm>({ resolver: zodResolver(goalSchema), defaultValues: defaultGoal() });
  const dreamFormState = useForm<DreamForm>({ resolver: zodResolver(dreamSchema), defaultValues: defaultDream() });
  useEffect(() => { goalFormState.reset(editing ? goalForm(editing) : defaultGoal()); }, [editing, goalFormState]);
  const goalMutation = useMutation({ mutationFn: (value: GoalRequest) => saveGoal(authResponse.data.csrf_token, value, editing?.id), onSuccess: () => { setNotice(editing ? "目标已更新。" : "目标已创建。"); setError(""); setEditing(null); void queryClient.invalidateQueries({ queryKey: ["user", accountID] }); }, onError: (value) => { setError(errorMessage(value)); setNotice(""); } });
  const dreamMutation = useMutation({ mutationFn: (value: DreamForm) => saveDream(authResponse.data.csrf_token, { title: value.title, description: value.description || null, goal_ids: [], sort_order: 0 }), onSuccess: () => { setNotice("梦想已保存。"); setError(""); dreamFormState.reset(defaultDream()); void queryClient.invalidateQueries({ queryKey: ["user", accountID, "dreams"] }); }, onError: (value) => { setError(errorMessage(value)); setNotice(""); } });
  const deleteMutation = useMutation({ mutationFn: ({ kind, id }: { kind: "goal" | "dream"; id: string }) => kind === "goal" ? deleteGoal(authResponse.data.csrf_token, id) : deleteDream(authResponse.data.csrf_token, id), onSuccess: () => { setNotice("已删除。目标子树和关联关系会同步清理。"); setError(""); setDeleteTarget(null); setSelected(null); void queryClient.invalidateQueries({ queryKey: ["user", accountID] }); }, onError: (value) => { setError(errorMessage(value)); setNotice(""); } });
  const goals = useMemo(() => goalsQuery.data?.data.items ?? [], [goalsQuery.data]);
  const nodes = useMemo(() => goalNodes(goals), [goals]);
  const edges = useMemo(() => goalEdges(goals), [goals]);
  const onGoalSubmit = goalFormState.handleSubmit((value) => { const metrics = value.metric_code ? [{ metric_code: value.metric_code as NonNullable<GoalRequest["metrics"]>[number]["metric_code"], target_value: value.target_value ?? 0, unit: value.unit || "次" }] : []; goalMutation.mutate({ title: value.title, type: value.type, parent_id: value.parent_id || null, description: null, start_date: value.start_date || null, due_date: value.due_date || null, status: value.status, sort_order: 0, metrics }); });
  return <div className="space-y-7"><PageHeader eyebrow="方向与执行" title="梦想与目标" description="用层级目标把长期方向拆成可行动的路径，实际进度由真实工作量和营业额自动计算。" />{(notice || error) && <p role={error ? "alert" : "status"} className={`rounded-md border px-4 py-3 text-sm ${error ? "border-rose-200 bg-rose-50 text-rose-800" : "border-teal-200 bg-teal-50 text-teal-900"}`}>{error || notice}</p>}<div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_360px]"><Panel title={editing ? "编辑目标" : "建立目标"} description="目标父子关系只能通过明确表单修改，不会因拖动画布误改变。" action={editing && <Button variant="ghost" size="sm" onClick={() => setEditing(null)}>取消编辑</Button>}><form className="space-y-4" onSubmit={onGoalSubmit}><Input label="目标名称" required error={goalFormState.formState.errors.title?.message} {...goalFormState.register("title")} /><div className="grid gap-3 sm:grid-cols-2"><label className="space-y-1.5"><span className="text-sm font-semibold text-slate-700">目标层级</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" {...goalFormState.register("type")} >{goalTypes.map((type) => <option key={type} value={type}>{typeLabels[type]}</option>)}</select></label><label className="space-y-1.5"><span className="text-sm font-semibold text-slate-700">父目标</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" {...goalFormState.register("parent_id")}><option value="">无父目标</option>{goals.filter((goal) => goal.id !== editing?.id).map((goal) => <option key={goal.id} value={goal.id}>{goal.title}</option>)}</select></label></div><div className="grid gap-3 sm:grid-cols-2"><Input label="开始日期" type="date" {...goalFormState.register("start_date")} /><Input label="截止日期" type="date" {...goalFormState.register("due_date")} /></div><label className="space-y-1.5"><span className="text-sm font-semibold text-slate-700">状态</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" {...goalFormState.register("status")} >{Object.entries(statusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><div className="border-t border-slate-100 pt-4"><p className="mb-3 text-sm font-bold text-slate-800">量化指标（可选）</p><div className="grid gap-3 sm:grid-cols-[1.4fr_0.7fr_0.7fr]"><label className="space-y-1.5"><span className="text-sm font-semibold text-slate-700">指标</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" {...goalFormState.register("metric_code")}><option value="">暂不设置</option>{metricCodes.map((code) => <option key={code} value={code}>{metricLabel(code)}</option>)}</select></label><Input label="目标值" type="number" min={0} step="0.01" {...goalFormState.register("target_value", { valueAsNumber: true })} /><Input label="单位" placeholder="次 / PV / 分钟" {...goalFormState.register("unit")} /></div></div><Button type="submit" loading={goalMutation.isPending}><Check size={16} />{goalMutation.isPending ? "处理中..." : editing ? "保存目标" : "创建目标"}</Button></form></Panel><Panel title="梦想板" description="梦想是长期方向，可以在后续完善图片和目标关联。"><form className="space-y-3" onSubmit={dreamFormState.handleSubmit((value) => dreamMutation.mutate(value))}><Input label="梦想标题" error={dreamFormState.formState.errors.title?.message} {...dreamFormState.register("title")} /><textarea className="min-h-20 w-full rounded-md border border-slate-300 px-3 py-2.5 text-sm" placeholder="描述这个方向" {...dreamFormState.register("description")} /><Button type="submit" variant="secondary" loading={dreamMutation.isPending}><Sparkles size={15} />保存梦想</Button></form><div className="mt-5 space-y-3">{dreamsQuery.data?.data.items.map((dream) => <div key={dream.id} className="flex items-start justify-between gap-3 border-t border-slate-100 pt-3"><div><p className="text-sm font-bold text-slate-900">{dream.title}</p>{dream.description && <p className="mt-1 text-xs text-slate-500">{dream.description}</p>}</div><Button variant="icon" size="sm" aria-label={`删除梦想 ${dream.title}`} onClick={() => setDeleteTarget({ kind: "dream", id: dream.id, title: dream.title })}><Trash2 size={15} /></Button></div>)}</div></Panel></div><Panel title="目标地图" description="父子目标以连线表达；节点可点击查看实时进度。"><div className="h-[520px] overflow-hidden rounded-md border border-slate-200 bg-slate-50">{goalsQuery.isPending ? <LoadingState label="正在加载目标" /> : goalsQuery.isError ? <ErrorState message="目标暂时无法加载" onRetry={() => void goalsQuery.refetch()} /> : goals.length ? <ReactFlow nodes={nodes} edges={edges} fitView minZoom={0.35} nodesDraggable={false} onNodeClick={(_, node) => setSelected(goals.find((goal) => goal.id === node.id) ?? null)}><MiniMap pannable zoomable /><Controls /><Background gap={20} size={1} /></ReactFlow> : <EmptyState title="还没有目标" description="创建第一个目标后，目标地图会展示清晰的执行路径。" />}</div>{selected && <div className="mt-4 rounded-md border border-teal-200 bg-teal-50/60 p-4"><div className="flex items-start justify-between gap-4"><div><p className="text-base font-bold text-slate-950">{selected.title}</p><p className="mt-1 text-sm text-slate-600">{typeLabels[selected.type]} · {selected.start_date || "未设置开始日期"} 至 {selected.due_date || "未设置截止日期"}</p></div><StatusBadge tone={statusTones[selected.status]}>{statusLabels[selected.status]}</StatusBadge></div><div className="mt-4 flex items-center justify-between"><span className="text-sm font-semibold text-slate-600">自动进度</span><strong className="text-teal-800">{Math.round(selected.progress * 100)}%</strong></div><div className="mt-2 h-2 rounded-full bg-white"><div className="h-full rounded-full bg-teal-600" style={{ width: `${selected.progress * 100}%` }} /></div>{selected.metrics.length > 0 && <div className="mt-4 space-y-2">{selected.metrics.map((metric) => <p key={metric.metric_code} className="text-xs text-slate-600">{metricLabel(metric.metric_code)}：{metric.actual_value} / {metric.target_value} {metric.unit}</p>)}</div>}<div className="mt-4 flex gap-2"><Button variant="secondary" size="sm" onClick={() => setEditing(selected)}>编辑目标</Button><Button variant="danger" size="sm" onClick={() => setDeleteTarget({ kind: "goal", id: selected.id, title: selected.title })}><Trash2 size={15} />删除目标</Button></div></div>}</Panel><Panel title="目标列表"><GoalList goals={goals} onEdit={setEditing} onDelete={(goal) => setDeleteTarget({ kind: "goal", id: goal.id, title: goal.title })} /></Panel><ConfirmDialog open={Boolean(deleteTarget)} onOpenChange={(open) => !open && setDeleteTarget(null)} title="确认删除" description={deleteTarget ? `确定要删除“${deleteTarget.title}”吗？${deleteTarget.kind === "goal" ? "它的子目标和指标也会被删除。" : "关联关系也会被删除。"}` : ""} confirmLabel="删除" loading={deleteMutation.isPending} onConfirm={() => deleteTarget && deleteMutation.mutate({ kind: deleteTarget.kind, id: deleteTarget.id })} /></div>;
}

function metricLabel(code: string): string { const labels: Record<string, string> = { conversation_open_count: "开启对话", deep_conversation_count: "深入对话", buffer_count: "Buffer", story_share_count: "分享故事", screening_count: "筛选", opportunity_count: "提供机会", meeting_count: "会面", customer_followup_count: "顾客跟进", reading_minutes: "读书分钟", audio_minutes: "听音频分钟", turnover_pv: "营业额 PV", turnover_net_amount: "净营业额" }; return labels[code] ?? code; }

function goalNodes(goals: Goal[]): Node<{ label: string; progress: number }>[] { const byID = new Map(goals.map((goal) => [goal.id, goal])); return goals.map((goal, index) => { let depth = 0; let parent = goal.parent_id; const visited = new Set<string>(); while (parent && byID.has(parent) && !visited.has(parent)) { visited.add(parent); depth += 1; parent = byID.get(parent)?.parent_id; } return { id: goal.id, position: { x: depth * 280, y: index * 115 }, data: { label: `${goal.title} · ${Math.round(goal.progress * 100)}%`, progress: goal.progress }, draggable: false }; }); }
function goalEdges(goals: Goal[]): Edge[] { return goals.filter((goal) => goal.parent_id).map((goal) => ({ id: `${goal.parent_id}-${goal.id}`, source: goal.parent_id!, target: goal.id, type: "smoothstep" })); }

function GoalList({ goals, onEdit, onDelete }: { goals: Goal[]; onEdit: (goal: Goal) => void; onDelete: (goal: Goal) => void }) { if (!goals.length) return <EmptyState title="还没有目标" description="创建目标后，它们会按照层级显示在这里。" />; return <div className="space-y-3">{goals.map((goal) => <div key={goal.id} className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 pb-3 last:border-0 last:pb-0"><div className="min-w-0"><p className="truncate text-sm font-bold text-slate-900">{goal.title}</p><p className="mt-1 text-xs text-slate-500">{typeLabels[goal.type]} · {goal.metrics.length ? `${goal.metrics.length} 个指标` : "无量化指标"}</p></div><div className="flex items-center gap-2"><StatusBadge tone={statusTones[goal.status]}>{statusLabels[goal.status]}</StatusBadge><span className="text-sm font-bold text-teal-700">{Math.round(goal.progress * 100)}%</span><Button variant="ghost" size="sm" onClick={() => onEdit(goal)}>编辑</Button><Button variant="icon" size="sm" aria-label={`删除目标 ${goal.title}`} onClick={() => onDelete(goal)}><Trash2 size={15} /></Button></div></div>)}</div>; }
