import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Background, Controls, ReactFlow } from "@xyflow/react";
import "@xyflow/react/dist/style.css";
import { Camera, Check, GitBranch, Trash2 } from "lucide-react";
import { useMemo, useState } from "react";
import type { AuthResponse, TeamMember, TeamMemberRequest } from "@/api/client";
import { createTeamSnapshot, deleteTeamMember, listTeamMembers, listTeamSnapshots, saveTeamMember } from "@/api/client";
import { Button } from "@/components/ui/button";
import { ConfirmDialog } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { PageHeader } from "@/components/ui/page-header";
import { Panel } from "@/components/ui/panel";
import { StatusBadge } from "@/components/ui/badge";
import { EmptyState, ErrorState, LoadingState } from "@/components/ui/state-block";
import { errorMessage } from "@/lib/utils";

type Form = { name: string; parent_id: string; rank: string; city: string; joined_on: string; status: "ACTIVE" | "INACTIVE"; note: string };
const emptyForm: Form = { name: "", parent_id: "", rank: "", city: "", joined_on: "", status: "ACTIVE", note: "" };

export function TeamPage({ authResponse }: { authResponse: AuthResponse }) {
  const accountId = authResponse.data.account.id;
  const client = useQueryClient();
  const membersQuery = useQuery({ queryKey: ["user", accountId, "team", "members"], queryFn: listTeamMembers });
  const snapshotsQuery = useQuery({ queryKey: ["user", accountId, "team", "snapshots"], queryFn: listTeamSnapshots });
  const members = useMemo(() => membersQuery.data?.data.items ?? [], [membersQuery.data]);
  const [form, setForm] = useState<Form>(emptyForm);
  const [editing, setEditing] = useState<TeamMember | null>(null);
  const [removeTarget, setRemoveTarget] = useState<TeamMember | null>(null);
  const [notice, setNotice] = useState("");
  const [error, setError] = useState("");
  const save = useMutation({ mutationFn: () => saveTeamMember(authResponse.data.csrf_token, formRequest(form), editing?.id), onSuccess: () => { setForm(emptyForm); setEditing(null); setNotice("团队成员已保存"); setError(""); void client.invalidateQueries({ queryKey: ["user", accountId, "team"] }); }, onError: (value) => { setError(errorMessage(value)); setNotice(""); } });
  const remove = useMutation({ mutationFn: () => deleteTeamMember(authResponse.data.csrf_token, removeTarget!.id), onSuccess: () => { setRemoveTarget(null); setNotice("团队成员已删除"); setError(""); void client.invalidateQueries({ queryKey: ["user", accountId, "team"] }); }, onError: (value) => setError(errorMessage(value)) });
  const snapshot = useMutation({ mutationFn: () => createTeamSnapshot(authResponse.data.csrf_token, { snapshot_type: "MANUAL", captured_late: false }), onSuccess: () => { setNotice("团队快照已保存"); void client.invalidateQueries({ queryKey: ["user", accountId, "team", "snapshots"] }); }, onError: (value) => setError(errorMessage(value)) });
  const nodes = useMemo(() => members.map((member, index) => ({ id: member.id, position: { x: (index % 3) * 220, y: Math.floor(index / 3) * 125 }, data: { label: <div className="min-w-[150px] rounded-md border border-teal-200 bg-white p-3 text-left shadow-sm"><p className="font-bold text-slate-900">{member.name}</p><p className="mt-1 text-xs text-slate-500">{member.rank || "未设置级别"} · {member.city || "未设置城市"}</p></div> }, type: "default" })), [members]);
  const edges = useMemo(() => members.filter((member) => member.parent_id).map((member) => ({ id: `${member.parent_id}-${member.id}`, source: member.parent_id!, target: member.id, type: "smoothstep" })), [members]);
  if (membersQuery.isPending) return <LoadingState label="正在加载团队" />;
  if (membersQuery.isError) return <ErrorState message="团队数据暂时无法加载" onRetry={() => void membersQuery.refetch()} />;
  const edit = (member: TeamMember) => setForm({ name: member.name, parent_id: member.parent_id ?? "", rank: member.rank ?? "", city: member.city ?? "", joined_on: member.joined_on ?? "", status: member.status, note: member.note ?? "" });
  return <div className="space-y-7"><PageHeader eyebrow="关系与成长" title="团队" description="维护当前团队结构，并用月末快照保留历史状态。" action={<Button onClick={() => snapshot.mutate()} loading={snapshot.isPending}><Camera size={16} />保存当前快照</Button>} />{(notice || error) && <p role={error ? "alert" : "status"} className={`rounded-md border px-4 py-3 text-sm ${error ? "border-rose-200 bg-rose-50 text-rose-800" : "border-teal-200 bg-teal-50 text-teal-900"}`}>{error || notice}</p>}<div className="grid gap-5 xl:grid-cols-[0.8fr_1.2fr]"><Panel title={editing ? "编辑成员" : "添加成员"} description="父节点通过明确表单调整，不会因拖动画布误改关系。"><div className="space-y-4"><Input label="团队成员姓名" required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /><label className="block space-y-1.5"><span className="text-sm font-semibold text-slate-700">上级成员</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" value={form.parent_id} onChange={(event) => setForm({ ...form, parent_id: event.target.value })}><option value="">直属根节点</option>{members.filter((member) => member.id !== editing?.id).map((member) => <option key={member.id} value={member.id}>{member.name}</option>)}</select></label><div className="grid gap-3 sm:grid-cols-2"><Input label="级别" value={form.rank} onChange={(event) => setForm({ ...form, rank: event.target.value })} /><Input label="城市" value={form.city} onChange={(event) => setForm({ ...form, city: event.target.value })} /><Input label="加入日期" type="date" value={form.joined_on} onChange={(event) => setForm({ ...form, joined_on: event.target.value })} /><label className="block space-y-1.5"><span className="text-sm font-semibold text-slate-700">状态</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as Form["status"] })}><option value="ACTIVE">启用</option><option value="INACTIVE">停用</option></select></label></div><label className="block space-y-1.5"><span className="text-sm font-semibold text-slate-700">备注</span><textarea className="min-h-20 w-full rounded-md border border-slate-300 px-3 py-2.5 text-sm" value={form.note} onChange={(event) => setForm({ ...form, note: event.target.value })} /></label><div className="flex justify-end gap-3"><Button variant="secondary" onClick={() => { setForm(emptyForm); setEditing(null); }}>清空</Button><Button onClick={() => save.mutate()} loading={save.isPending}><Check size={15} />保存成员</Button></div></div></Panel><Panel title="团队关系图" description="大屏显示关系画布，移动端可横向查看。"><div className="h-[430px] rounded-md border border-slate-200 bg-slate-50">{nodes.length ? <ReactFlow nodes={nodes} edges={edges} fitView nodesDraggable={false}><Background /><Controls /></ReactFlow> : <div className="grid h-full place-items-center"><EmptyState title="还没有团队成员" description="添加第一位成员后会显示关系图。" /></div>}</div></Panel></div><Panel title="成员列表" description="成员删除默认阻止存在子节点的情况，需先调整层级。">{members.length ? <div className="space-y-3">{members.map((member) => <div key={member.id} className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 pb-3 last:border-0"><div><p className="font-bold text-slate-900">{member.name}</p><p className="mt-1 text-xs text-slate-500">{member.rank || "未设置级别"} · {member.city || "未设置城市"}</p></div><div className="flex items-center gap-2"><StatusBadge tone={member.status === "ACTIVE" ? "success" : "neutral"}>{member.status === "ACTIVE" ? "启用" : "停用"}</StatusBadge><Button variant="secondary" size="sm" onClick={() => { setEditing(member); edit(member); }}>编辑</Button><Button variant="icon" size="sm" aria-label={`删除成员 ${member.name}`} onClick={() => setRemoveTarget(member)}><Trash2 size={15} /></Button></div></div>)}</div> : <EmptyState title="暂无成员" description="从左侧添加团队成员。" />}</Panel><Panel title="历史快照" description="快照只读，后续成员改名或删除不会影响历史记录。">{snapshotsQuery.data?.data.items.length ? <div className="grid gap-3 sm:grid-cols-2">{snapshotsQuery.data.data.items.map((item) => <div key={item.id} className="rounded-md border border-slate-200 p-4"><div className="flex items-center gap-2"><GitBranch size={16} className="text-teal-700" /><p className="font-semibold">{item.snapshot_month}</p></div><p className="mt-2 text-xs text-slate-500">{item.members.length} 位成员 · {item.snapshot_type === "AUTO" ? "自动" : "手动"}</p></div>)}</div> : <EmptyState title="暂无历史快照" description="保存一次当前快照后会出现在这里。" />}</Panel><ConfirmDialog open={Boolean(removeTarget)} onOpenChange={(open) => !open && setRemoveTarget(null)} title="确认删除成员" description={removeTarget ? `确定删除“${removeTarget.name}”吗？存在下属时需要先调整层级。` : ""} confirmLabel="删除" loading={remove.isPending} onConfirm={() => remove.mutate()} /></div>;
}

function formRequest(form: Form): TeamMemberRequest { return { name: form.name.trim(), parent_id: form.parent_id || null, rank: form.rank.trim() || null, city: form.city.trim() || null, joined_on: form.joined_on || null, status: form.status, note: form.note.trim() || null, sort_order: 0 }; }
