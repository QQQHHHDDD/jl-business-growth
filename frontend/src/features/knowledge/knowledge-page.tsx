import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { BookOpen, Check, FileUp, Timer, Trash2 } from "lucide-react";
import { useState } from "react";
import type { AuthResponse, KnowledgeItem, KnowledgeItemRequest } from "@/api/client";
import { deleteKnowledgeItem, listFiles, listKnowledgeItems, listLearningSessions, saveKnowledgeItem, saveLearningSession, uploadFile } from "@/api/client";
import { Button } from "@/components/ui/button";
import { ConfirmDialog } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { PageHeader } from "@/components/ui/page-header";
import { Panel } from "@/components/ui/panel";
import { StatusBadge } from "@/components/ui/badge";
import { EmptyState, ErrorState, LoadingState } from "@/components/ui/state-block";
import { businessDate } from "@/lib/date";
import { errorMessage } from "@/lib/utils";

type Form = { title: string; type: KnowledgeItemRequest["type"]; status: KnowledgeItemRequest["status"]; raw_text: string; tags: string; progress_current: string; progress_total: string; progress_unit: string };
const blank: Form = { title: "", type: "BOOK", status: "NOT_STARTED", raw_text: "", tags: "", progress_current: "", progress_total: "", progress_unit: "页" };

export function KnowledgePage({ authResponse }: { authResponse: AuthResponse }) {
  const accountId = authResponse.data.account.id;
  const client = useQueryClient();
  const itemsQuery = useQuery({ queryKey: ["user", accountId, "knowledge"], queryFn: listKnowledgeItems });
  const filesQuery = useQuery({ queryKey: ["user", accountId, "files"], queryFn: listFiles });
  const sessionsQuery = useQuery({ queryKey: ["user", accountId, "learning-sessions"], queryFn: () => listLearningSessions() });
  const [form, setForm] = useState<Form>(blank); const [editing, setEditing] = useState<KnowledgeItem | null>(null); const [removeTarget, setRemoveTarget] = useState<KnowledgeItem | null>(null); const [selectedFile, setSelectedFile] = useState<File | null>(null); const [notice, setNotice] = useState(""); const [error, setError] = useState("");
  const save = useMutation({ mutationFn: () => saveKnowledgeItem(authResponse.data.csrf_token, itemRequest(form), editing?.id), onSuccess: () => { setForm(blank); setEditing(null); setNotice("学习项目已保存"); setError(""); void client.invalidateQueries({ queryKey: ["user", accountId, "knowledge"] }); }, onError: (value) => { setError(errorMessage(value)); setNotice(""); } });
  const remove = useMutation({ mutationFn: () => deleteKnowledgeItem(authResponse.data.csrf_token, removeTarget!.id), onSuccess: () => { setRemoveTarget(null); setNotice("学习项目已删除"); void client.invalidateQueries({ queryKey: ["user", accountId, "knowledge"] }); }, onError: (value) => setError(errorMessage(value)) });
  const upload = useMutation({ mutationFn: () => uploadFile(authResponse.data.csrf_token, selectedFile!, selectedFile?.type.startsWith("image/") ? "KNOWLEDGE_IMAGE" : "KNOWLEDGE_DOCUMENT"), onSuccess: () => { setSelectedFile(null); setNotice("文件已上传，正文不会被自动解析。"); void client.invalidateQueries({ queryKey: ["user", accountId, "files"] }); }, onError: (value) => setError(errorMessage(value)) });
  const session = useMutation({ mutationFn: (knowledgeItemId: string) => saveLearningSession(authResponse.data.csrf_token, { knowledge_item_id: knowledgeItemId, activity_type: "READING", activity_date: businessDate(authResponse.data.account.timezone), minutes: 30, source: "ITEM" }), onSuccess: () => { setNotice("已记录 30 分钟读书时间"); void client.invalidateQueries({ queryKey: ["user", accountId, "learning-sessions"] }); }, onError: (value) => setError(errorMessage(value)) });
  if (itemsQuery.isPending) return <LoadingState label="正在加载学习中心" />;
  if (itemsQuery.isError) return <ErrorState message="学习项目暂时无法加载" onRetry={() => void itemsQuery.refetch()} />;
  const edit = (item: KnowledgeItem) => { setEditing(item); setForm({ title: item.title, type: item.type, status: item.status, raw_text: item.raw_text ?? "", tags: item.tags.join(", "), progress_current: item.progress_current?.toString() ?? "", progress_total: item.progress_total?.toString() ?? "", progress_unit: item.progress_unit ?? "页" }); };
  return <div className="space-y-7"><PageHeader eyebrow="输入与沉淀" title="学习中心" description="学习项目和读书/音频时长使用同一份事实源；附件只做安全存储与网页预览。" />{(notice || error) && <p role={error ? "alert" : "status"} className={`rounded-md border px-4 py-3 text-sm ${error ? "border-rose-200 bg-rose-50 text-rose-800" : "border-teal-200 bg-teal-50 text-teal-900"}`}>{error || notice}</p>}<div className="grid gap-5 xl:grid-cols-[0.85fr_1.15fr]"><Panel title={editing ? "编辑学习项目" : "新增学习项目"} description="原始文字只来自手工输入，不从附件中提取。"><div className="space-y-4"><Input label="学习项目标题" required value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} /><div className="grid gap-3 sm:grid-cols-2"><label className="block space-y-1.5"><span className="text-sm font-semibold text-slate-700">类型</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" value={form.type} onChange={(event) => setForm({ ...form, type: event.target.value as Form["type"] })}>{["BOOK","AUDIO","VIDEO","EVENT","MEETING","PHP","MENTOR","PRODUCT","OTHER"].map((value) => <option key={value} value={value}>{value}</option>)}</select></label><label className="block space-y-1.5"><span className="text-sm font-semibold text-slate-700">状态</span><select className="min-h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm" value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as Form["status"] })}><option value="NOT_STARTED">未开始</option><option value="IN_PROGRESS">进行中</option><option value="COMPLETED">已完成</option></select></label></div><div className="grid gap-3 sm:grid-cols-3"><Input label="当前进度" type="number" min="0" value={form.progress_current} onChange={(event) => setForm({ ...form, progress_current: event.target.value })} /><Input label="总进度" type="number" min="0" value={form.progress_total} onChange={(event) => setForm({ ...form, progress_total: event.target.value })} /><Input label="进度单位" value={form.progress_unit} onChange={(event) => setForm({ ...form, progress_unit: event.target.value })} /></div><Input label="标签（逗号分隔）" value={form.tags} onChange={(event) => setForm({ ...form, tags: event.target.value })} /><label className="block space-y-1.5"><span className="text-sm font-semibold text-slate-700">学习内容</span><textarea className="min-h-28 w-full rounded-md border border-slate-300 px-3 py-2.5 text-sm" value={form.raw_text} onChange={(event) => setForm({ ...form, raw_text: event.target.value })} /></label><div className="flex justify-end gap-3"><Button variant="secondary" onClick={() => { setForm(blank); setEditing(null); }}>清空</Button><Button onClick={() => save.mutate()} loading={save.isPending}><Check size={15} />保存项目</Button></div></div></Panel><Panel title="学习项目" description="为项目快速记录读书时长，后续可从项目详情持续补充。">{itemsQuery.data?.data.items.length ? <div className="space-y-3">{itemsQuery.data.data.items.map((item) => <div key={item.id} className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 pb-3 last:border-0"><div><div className="flex items-center gap-2"><BookOpen size={16} className="text-teal-700" /><p className="font-bold text-slate-900">{item.title}</p></div><p className="mt-1 text-xs text-slate-500">{item.type} · {item.tags.join("、") || "无标签"}</p></div><div className="flex items-center gap-2"><StatusBadge tone={item.status === "COMPLETED" ? "success" : "info"}>{item.status === "COMPLETED" ? "已完成" : item.status === "IN_PROGRESS" ? "进行中" : "未开始"}</StatusBadge><Button variant="secondary" size="sm" onClick={() => session.mutate(item.id)} loading={session.isPending}><Timer size={14} />读 30 分钟</Button><Button variant="secondary" size="sm" onClick={() => edit(item)}>编辑</Button><Button variant="icon" size="sm" aria-label={`删除学习项目 ${item.title}`} onClick={() => setRemoveTarget(item)}><Trash2 size={15} /></Button></div></div>)}</div> : <EmptyState title="还没有学习项目" description="建立一条读书、音频或活动记录开始沉淀。" />}</Panel></div><div className="grid gap-5 xl:grid-cols-2"><Panel title="文件资产" description="仅支持 PDF、TXT、Markdown、JPG、PNG、WebP；附件正文不参与搜索。"><div className="flex flex-wrap items-end gap-3"><label className="min-w-[240px] flex-1 space-y-1.5"><span className="text-sm font-semibold text-slate-700">选择文件</span><input aria-label="选择学习文件" type="file" accept="application/pdf,text/plain,text/markdown,image/jpeg,image/png,image/webp,.md" className="block w-full text-sm" onChange={(event) => setSelectedFile(event.target.files?.[0] ?? null)} /></label><Button onClick={() => upload.mutate()} disabled={!selectedFile} loading={upload.isPending}><FileUp size={16} />上传文件</Button></div>{filesQuery.data?.data.items.length ? <div className="mt-5 space-y-2">{filesQuery.data.data.items.map((file) => <a key={file.id} href={`/api/files/${file.id}/content?disposition=inline`} target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-md border border-slate-200 px-3 py-2 text-sm hover:border-teal-300"><span className="truncate font-semibold text-slate-800">{file.original_name}</span><span className="text-xs text-slate-500">{Math.ceil(file.size_bytes / 1024)} KB</span></a>)}</div> : <p className="mt-4 text-sm text-slate-500">尚未上传文件。</p>}</Panel><Panel title="近期学习时长" description="每日工作量和学习项目都会汇总到这里，不会重复累计。">{sessionsQuery.data?.data.items.length ? <div className="space-y-3">{sessionsQuery.data.data.items.slice(0, 8).map((item) => <div key={item.id} className="flex items-center justify-between border-b border-slate-100 pb-3 last:border-0"><span className="text-sm font-semibold text-slate-800">{item.activity_date} · {item.activity_type === "READING" ? "读书" : "音频"}</span><span className="text-sm font-bold text-teal-700">{item.minutes} 分钟</span></div>)}</div> : <EmptyState title="暂无学习时长" description="在学习项目中记录读书，或从每日工作量填写分钟数。" />}</Panel></div><ConfirmDialog open={Boolean(removeTarget)} onOpenChange={(open) => !open && setRemoveTarget(null)} title="确认删除学习项目" description={removeTarget ? `确定删除“${removeTarget.title}”吗？关联的项目学习记录也会删除。` : ""} confirmLabel="删除" loading={remove.isPending} onConfirm={() => remove.mutate()} /></div>;
}

function itemRequest(form: Form): KnowledgeItemRequest { const current = form.progress_current === "" ? null : Number(form.progress_current); const total = form.progress_total === "" ? null : Number(form.progress_total); return { title: form.title.trim(), type: form.type, status: form.status, raw_text: form.raw_text.trim() || null, tags: form.tags.split(",").map((item) => item.trim()).filter(Boolean), progress_current: current, progress_total: total, progress_unit: form.progress_unit.trim() || null, file_ids: [] }; }
