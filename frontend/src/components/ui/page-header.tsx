import type { ReactNode } from "react";
import { cn } from "@/lib/utils";

export function PageHeader({ eyebrow, title, description, action, className }: { eyebrow?: string; title: string; description?: string; action?: ReactNode; className?: string }) {
  return <div className={cn("page-header relative isolate flex min-w-0 flex-col gap-4 overflow-hidden rounded-hero border border-brand-100/80 bg-gradient-to-br from-surface via-brand-50/35 to-sky-50/45 px-5 py-5 shadow-card sm:flex-row sm:items-end sm:justify-between sm:px-7 sm:py-6", className)}><span className="page-header-wash pointer-events-none absolute -right-16 -top-20 h-52 w-52 rounded-full bg-brand-100/45 blur-3xl" aria-hidden="true" /><span className="page-header-rule pointer-events-none absolute bottom-0 right-[20%] h-px w-40 bg-gradient-to-r from-transparent via-brand-300/70 to-transparent" aria-hidden="true" /><div className="relative z-[1] min-w-0">{eyebrow && <p className="text-xs font-bold tracking-[0.08em] text-brand-700">{eyebrow}</p>}<h1 className="mt-1 text-[32px] font-extrabold leading-[1.12] tracking-[-0.03em] text-ink">{title}</h1>{description && <p className="mt-2 max-w-2xl text-sm leading-6 text-ink-muted">{description}</p>}</div>{action && <div className="relative z-[1] flex max-w-full shrink-0 flex-wrap items-center gap-2">{action}</div>}</div>;
}
